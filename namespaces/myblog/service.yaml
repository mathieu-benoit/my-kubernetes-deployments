apiVersion: v1
kind: Service
metadata:
  name: myblog
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
  labels:
    app: myblog
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: myblog