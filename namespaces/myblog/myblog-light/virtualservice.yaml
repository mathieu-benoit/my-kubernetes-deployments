apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: myblog
spec:
  hosts:
    - light.alwaysupalwayson.com
  gateways:
  - asm-ingress/asm-ingressgateway
  http:
  - route:
    - destination:
        host: myblog
        port:
          number: 80