apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: myblog
spec:
  hosts:
    - "*"
  gateways:
  - myblog
  http:
  - route:
    - destination:
        host: myblog
        port:
          number: 80